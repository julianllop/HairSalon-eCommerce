import React from "react";
import Calendar from "../Components/calendar/calendar";

const Appointments = () => {
    return (
        <div>
            <Calendar />
        </div>
    );
};

export default Appointments;
